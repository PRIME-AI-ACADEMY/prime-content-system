# 03 · Генерация медиа локально

**Назначение.** Пока ноутбук открыт, рутина делает то, что облако не умеет: карусели кодом (маршрут C, `tools/build_c.py`, обложка и финал с твоими фото из `photos/`), рилсы из твоих клипов (`Reels · M`, `tools/build_reel.py`) и говорящую голову (`Reels · M · говорящая голова`, HyperFrames, скилл `reels-montazh`). Горизонт — 7 дней вперёд (`ГОРИЗОНТ: 7 дней`). Складывает в `output/` и в Drive, ставит «На утверждении».

Хочешь все карусели на неделю за один раз — поставь эту же рутину на понедельник 07:30 с `ГОРИЗОНТ: 7 дней`; ежедневный запуск с тем же горизонтом добирает новые строки.

**Когда.** Ежедневно 07:30 (после облачной 02). **Где.** 💻 Локально, приложение Claude Code.

**Что нужно заранее**
- Папка завода с `.claude/settings.local.json` из `templates/settings.local.safe.json` (allow-лист команд) **и реальными id коннекторов `mcp__<uuid>`** (их дописывает `setup-check`). Рутину создаёшь ИЗ ЭТОЙ папки. Рутина всегда идёт в режиме Manual: спасают только allow-лист и простые команды в промте. Для монтажа в allow-листе нужны `npx`, `npm`, `node`, `whisper-cli`, `touch`, `cd`, `rm`.
- В папке: `profile/design-system.md`, `photos/` + `photo-catalog.md`, `fonts/Montserrat-VF.ttf` (или свой шрифт с кириллицей), `clips/` с 3–10 вертикальными клипами.
- Python + Pillow + `opencv-python-headless<5` + `fonts/haarcascade_frontalface_default.xml`, ffmpeg, `tools/build_c.py` + `tools/lab_common.py`, `tools/build_reel.py`. Для говорящей головы: Node.js, скиллы HyperFrames и Remotion, проект `reels-hf/`, whisper-cli с моделью (шаг «монтаж» в ПРОМТ-УЧАСТНИКА).
- Коннекторы: Composio (Sheets + Drive), Higgsfield (только для публичной ссылки на файл).
- Первый запуск — Run now при тебе.

---

## ПРОМТ ДЛЯ АНГЕЛИНЫ

```
Ты — рутина «03 медиа локально» контент-завода Angelina (PRIME AI). Работай сама,
ничего не спрашивай, ничего не публикуй. Ты в папке завода. Команды только простые:
одна команда — одно действие, без $(...), без > и >>. Файлы пиши инструментом записи.

ДАННЫЕ
- Таблица 1sdIQe_iHqAk49Bb1byY6OittHvtm2US3Hgb7s7xUCPo, вкладка «Контент-план».
  A Дата и время · B Формат · C Тема · F CTA · G Промпт на каждый слайд · H Текст слайдов /
  Voiceover + заметка о медиа (фото/клип/скрин) · I Воронка · J Функция · Этап · Точка
  назначения · K Ссылка на медиа · L Статус · N Зашло.
- Локально: profile/design-system.md, profile/business-dna.md («Актуальный запуск»),
  profile/funnels.md, photos/ + photo-catalog.md, fonts/Montserrat-VF.ttf,
  fonts/haarcascade_frontalface_default.xml, clips/ (исходники рилсов), output/,
  tools/build_c.py, tools/build_reel.py, reels-hf/ (HyperFrames), faces.json.
- Drive: «Контент-завод» → Готовое. Публичная ссылка на файл — мост drive-bridge:
  Higgsfield media_upload → curl -X PUT -H "Content-Type: <тип>" --data-binary @файл
  <upload_url> → (media_confirm) → CloudFront-URL → Composio UPLOAD_FROM_URL.
- Бренд: из profile/design-system.md. Ник @angelinaa.kh. Кодовые слова — из
  «Актуальный запуск». ГЕЙТ: да. ГОРИЗОНТ: 7 дней.

ШАГИ
1. Прочитай таблицу. Возьми строки, где: дата A от сегодня до сегодня + ГОРИЗОНТ − 1 день
   включительно; K пустая; L = «План»; B содержит «· C» ИЛИ B начинается с «Reels · M».
   Строки «· H», «· K», «Reels», «Reels · S» — пропусти (облако). Если в N есть «Ошибка»
   с сегодняшней датой — пропусти. Подходящих нет — напиши «нет строк» и закончи.
2. КАРУСЕЛЬ · C (T1 текст / T3 скрины / T4 инфографика):
   - Собери JSON для tools/build_c.py по G и H: "brand": "profile/design-system.md";
     обложка (role cover) и финал (role cta) — ОБЯЗАТЕЛЬНО "photo": файл из photos/ по
     заметке в H и photo-catalog.md (лицо целиком, HEIC сначала sips -s format jpeg);
     средние слайды — big/small/number (T1), screenshot (T3), compare (T4); cta из F.
     JSON пиши инструментом записи файлов в output/ДД.ММ-<тема>/spec.json.
   - python3 tools/build_c.py output/ДД.ММ-<тема>/spec.json --out output --name ДД.ММ-<тема>
     → slide-01..NN.png, 00-contact.jpg, 00-check.jpg, report.md. Код 3 — другое фото или
     focus; 4 — контраст; 6 — нет детектора лиц, остановись на этой строке и напиши в отчёт.
     Открой 00-check.jpg: текст = H, не на лице, ничего не вылезло. Брак — поправь JSON.
3. REELS · M (клип + хук):
   - Выбери клип из clips/ по заметке в H или первый неиспользованный (output/used-clips.txt;
     нет файла — считай все свободными). Из H возьми: хук (l1, l2), подпись (sub), кикер, CTA.
   - Запусти python3 tools/build_reel.py --src <клип> --out output/ДД.ММ-<тема>/reel.mp4
     --kicker … --l1 … --l2 … --sub … --cta "<CTA из F>" --handle "@angelinaa.kh".
     Посмотри reel.check.jpg: текст не на лице. Код 3 или 4 — другой клип, повтори 1 раз
     (Haar находит «лица» на листьях: клип без человека → --no-face-check и проверь
     check.jpg глазами). Код 5 — нет детектора, остановись на строке, напиши в отчёт.
   - Допиши имя клипа в output/used-clips.txt инструментом записи файлов.
3а. REELS · M · говорящая голова (по скиллу reels-montazh, трек Б):
   - Исходник — файл из clips/ по заметке в H (длинная запись 30–90 с). Паузы:
     silencedetect noise=-45dB:d=0.4, режь только паузы > 0.4 с с запасом 0.2 с, сверь
     с таймкодами whisper → cut.mp4. Транскрипт whisper-cli с cut.mp4, модель medium, -l ru.
   - Композиция в reels-hf/ скриптом: субтитры фразами над головой по faces.json, 2–4
     вставки по тезисам из G, чипы на торс, зум 1.0→1.06, @ник, прогресс-бар, CTA-плашка
     top ≈1120 (не на нике). На контейнер с зумом — data-layout-allow-overflow.
   - npm run check → snapshot на 5 таймкодах (текст не на лице) → npm run render.
   - Финал ВСЕГДА перекодируй в H.264: ffmpeg -i render.mp4 -c:v h264_videotoolbox -b:v 8M
     -movflags +faststart -pix_fmt yuv420p -c:a aac out.mp4 (Windows: libx264 -crf 21).
     ffprobe: есть звук, длительность = cut.mp4.
4. Каждый готовый файл: media_upload (files[], до 20 за раз) → curl PUT с Content-Type
   (image/png, video/mp4) → media_confirm, если просит → CloudFront-ссылка.
   Затем Composio UPLOAD_FROM_URL в папку Drive «Готовое/ДД.ММ — <тема>», доступ anyone/reader.
5. В строку: K = ссылка на папку Drive, с новой строки CloudFront-ссылки по порядку
   (их берёт Postiz). L = «На утверждении» (ГЕЙТ: нет → «Утверждено»).
6. Отчёт: строки, файлы, ссылки, что пересобирали.

ОШИБКИ
- Ошибка на строке (нет Pillow, ffmpeg, фото, клипов) → N = «Ошибка ДД.ММ: коротко»,
  L не меняй, дальше. Нет клипов вообще — пропусти все Reels · M и напиши об этом.
- Не залилось в Drive → K = локальный путь output/…, L = «На утверждении», отметь в отчёте.
- Никогда не завершай молча.
```

---

## ПРОМТ ДЛЯ УЧАСТНИКА

```
Ты — рутина «03 медиа локально» моего контент-завода. Работай сама, ничего не
спрашивай, ничего не публикуй. Ты в папке завода. Команды только простые:
одна команда — одно действие, без $(...), без > и >>. Файлы пиши инструментом записи.

ДАННЫЕ
- Таблица [ID таблицы], вкладка «Контент-план».
  A Дата и время · B Формат · C Тема · F CTA · G Промпт на каждый слайд · H Текст слайдов /
  Voiceover + заметка о медиа (фото/клип/скрин) · I Воронка · J Функция · Этап · Точка
  назначения · K Ссылка на медиа · L Статус · N Зашло.
- Локально: profile/design-system.md, profile/business-dna.md («Актуальный запуск»),
  profile/funnels.md, photos/ + photo-catalog.md, fonts/ (шрифт с кириллицей +
  haarcascade_frontalface_default.xml), clips/ (исходники рилсов), output/,
  tools/build_c.py + tools/lab_common.py, tools/build_reel.py
  (Windows: python вместо python3), reels-hf/ (если ставили монтаж).
- Drive: [ссылка на папку Drive] → Готовое. Публичная ссылка на файл — мост drive-bridge:
  Higgsfield media_upload → curl -X PUT -H "Content-Type: <тип>" --data-binary @файл
  <upload_url> → (media_confirm) → CloudFront-URL → Composio UPLOAD_FROM_URL.
- Бренд: из profile/design-system.md. Ник [ник]. Кодовые слова — из «Актуальный запуск».
  ГЕЙТ: да. ГОРИЗОНТ: 7 дней.

ШАГИ
1. Прочитай таблицу. Возьми строки, где: дата A от сегодня до сегодня + ГОРИЗОНТ − 1 день
   включительно; K пустая; L = «План»; B содержит «· C» ИЛИ B начинается с «Reels · M».
   Строки «· H», «· K», «Reels», «Reels · S» — пропусти (облако). Если в N есть «Ошибка»
   с сегодняшней датой — пропусти. Подходящих нет — напиши «нет строк» и закончи.
2. КАРУСЕЛЬ · C (T1 текст / T3 скрины / T4 инфографика):
   - Собери JSON для tools/build_c.py по G и H: "brand": "profile/design-system.md";
     обложка (role cover) и финал (role cta) — ОБЯЗАТЕЛЬНО "photo": файл из photos/ по
     заметке в H и photo-catalog.md (лицо целиком, HEIC сначала sips -s format jpeg на Mac,
     на Windows — Pillow + pillow-heif); средние слайды — big/small/number (T1),
     screenshot (T3), compare (T4); cta из F; handle [ник].
     JSON пиши инструментом записи файлов в output/ДД.ММ-<тема>/spec.json.
   - python3 tools/build_c.py output/ДД.ММ-<тема>/spec.json --out output --name ДД.ММ-<тема>
     → slide-01..NN.png, 00-contact.jpg, 00-check.jpg, report.md. Код 3 — другое фото или
     focus; 4 — контраст; 6 — нет детектора лиц, остановись на этой строке и напиши в отчёт.
     Открой 00-check.jpg: текст = H, не на лице, ничего не вылезло. Брак — поправь JSON.
3. REELS · M (клип + хук):
   - Выбери клип из clips/ по заметке в H или первый неиспользованный (output/used-clips.txt;
     нет файла — считай все свободными). Из H возьми: хук (l1, l2), подпись (sub), кикер, CTA.
   - Запусти python3 tools/build_reel.py --src <клип> --out output/ДД.ММ-<тема>/reel.mp4
     --kicker … --l1 … --l2 … --sub … --cta "<CTA из F>" --handle "[ник]".
     Посмотри reel.check.jpg: текст не на лице. Код 3 или 4 — другой клип, повтори 1 раз
     (Haar находит «лица» на листьях: клип без человека → --no-face-check и проверь
     check.jpg глазами). Код 5 — нет детектора, остановись на строке, напиши в отчёт.
   - Допиши имя клипа в output/used-clips.txt инструментом записи файлов.
3а. REELS · M · говорящая голова (только если стоит монтаж: reels-hf/ и whisper;
   иначе N = «Ошибка ДД.ММ: нет монтажа», строку пропусти). По скиллу reels-montazh, трек Б:
   - Исходник — файл из clips/ по заметке в H (запись 30–90 с). Паузы: silencedetect
     noise=-45dB:d=0.4, режь только паузы > 0.4 с с запасом 0.2 с, сверь с таймкодами
     whisper → cut.mp4. Транскрипт с cut.mp4: whisper-cli, модель medium, язык флагом
     (Windows: npx hyperframes transcribe).
   - Композиция в reels-hf/ скриптом: субтитры фразами над головой по faces.json, 2–4
     вставки по тезисам из G, чипы на торс, зум 1.0→1.06, @ник, прогресс-бар, CTA-плашка
     top ≈1120 (не на нике). На контейнер с зумом — data-layout-allow-overflow.
   - npm run check → snapshot на 5 таймкодах (текст не на лице) → npm run render.
   - Финал ВСЕГДА перекодируй в H.264: ffmpeg -i render.mp4 -c:v h264_videotoolbox -b:v 8M
     -movflags +faststart -pix_fmt yuv420p -c:a aac out.mp4 (Windows: libx264 -crf 21).
     ffprobe: есть звук, длительность = cut.mp4.
4. Каждый готовый файл: media_upload (files[], до 20 за раз) → curl PUT с Content-Type
   (image/png, video/mp4) → media_confirm, если просит → CloudFront-ссылка.
   Затем Composio UPLOAD_FROM_URL в папку Drive «Готовое/ДД.ММ — <тема>», доступ anyone/reader.
5. В строку: K = ссылка на папку Drive, с новой строки CloudFront-ссылки по порядку
   (их берёт Postiz). L = «На утверждении» (ГЕЙТ: нет → «Утверждено»).
6. Отчёт: строки, файлы, ссылки, что пересобирали.

ОШИБКИ
- Ошибка на строке (нет Pillow, ffmpeg, фото, клипов) → N = «Ошибка ДД.ММ: коротко»,
  L не меняй, дальше. Нет клипов вообще — пропусти все Reels · M и напиши об этом.
- Не залилось в Drive → K = локальный путь output/…, L = «На утверждении», отметь в отчёте.
- Никогда не завершай молча.
```

---

## Как поставить (отличается от облачных)
1. Открой приложение Claude Code в папке завода. Проверь, что там лежит `.claude/settings.local.json` из `templates/settings.local.safe.json`. Нет — скажи: «скопируй шаблон templates/settings.local.safe.json из плагина в .claude/settings.local.json» и открой новый чат.
2. В чате: «создай рутину «03 медиа локально», ежедневно 07:30, вот промт:» + промт.
3. Routines → Run now, посиди рядом. Коннекторы спросят по разу — «Always allow» (запоминается). Если спросила про shell-команду — это «Allow once», запомнить нельзя: найди эту команду в промте и упрости (без `$(…)`, `>`, `;`), пересоздай рутину. Дошла до отчёта без вопросов — готово.

Четыре правила, почему рутина не встаёт: allow-лист в папке · простые команды · первый Run now при тебе · всё остальное в облаке (00-КАРТА).

## Как проверить, что сработало
- В `output/ДД.ММ-тема/` лежат PNG или reel.mp4 + out.check.jpg (текст не на лице).
- В Drive та же папка в «Готовое», в таблице K и L = «На утверждении».
- Рутина ни разу не спросила «Разрешить?».

## Типовые ошибки
| Что видишь | Причина | Что делать |
|---|---|---|
| Рутина висит, ждёт «Разрешить?» на команду | создана из другой папки (нет allow-листа) или в промте `$(…)`, `>`, `;` | пересоздай из папки завода с `settings.local.safe.json`; упрости команду в промте |
| Спрашивает про коннектор | первый вызов инструмента | «Always allow» один раз, дальше молчит |
| Квадраты вместо букв | шрифт без кириллицы | положи Montserrat-VF.ttf с Google Fonts в fonts/ |
| `No module named PIL` | нет Pillow | `python3 -m pip install --user pillow` (Windows: `python`) |
| Код 6 / код 5 «нет детектора» | OpenCV 5.x или нет каскада | `pip install "opencv-python-headless<5"`, каскад в `fonts/` |
| Рутина встала на `npx` / коннекторе | allow-лист без `npx` или без id `mcp__<uuid>` | обнови `.claude/settings.local.json` из шаблона, запусти `setup-check` |
| Рилс не грузится в Postiz / Instagram | HEVC на выходе HyperFrames | шаг 3а: перекодировать в H.264 |
| Текст на лице в рилсе | клип с лицом на весь кадр | другой клип, общий план |
| «Ошибка … нет клипов» | пустая clips/ | положи 3–10 вертикальных клипов |
| Сделала строку «· H» | старый промт | шаг 1: «· H» пропускается |
